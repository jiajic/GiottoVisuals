% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_scatter.R
\name{scatterPlot}
\alias{scatterPlot}
\title{Create a Scatter Plot}
\usage{
scatterPlot(
  gobject,
  feat1,
  feat2,
  stat = c("point", "hexbin", "density", "contour"),
  color = NULL,
  fill = NULL,
  size = 1,
  stroke = 1,
  alpha = 1,
  shape = 21,
  jitter = FALSE,
  as_factor = NULL,
  transforms = NULL,
  show_legend = FALSE,
  theme_param = list(),
  cow_n_col = NULL,
  cow_rel_h = 1,
  cow_rel_w = 1,
  cow_align = "h",
  show_plot = NULL,
  return_plot = NULL,
  save_plot = NULL,
  save_param = list(),
  default_save_name = "scatterPlot",
  verbose = NULL
)
}
\arguments{
\item{gobject}{giotto object}

\item{feat1}{character or \code{svkey}. Continuous values only.}

\item{feat2}{character or \code{svkey}. Continuous values only.}

\item{stat}{character. Statistic used when plotting. One of \code{"point"},
\code{"hexbin"}, \code{"density"}, or \code{"contour"}. When not using \code{"point"}, \code{fill},
shape,}

\item{color}{character or \code{svkey}. Categorical or continuous values.}

\item{fill}{character or \code{svkey}. Categorical or continuous values. Only
visible for \code{shape} values 21-25. This aesthetic is ignored if multiple
\code{shape} values are used.}

\item{size}{numeric or \code{svkey}. Continuous values only.}

\item{stroke}{numeric or \code{svkey}. Continuous values only. Only used if
\code{shape} is within 21-25}

\item{alpha}{numeric or \code{svkey}. Continuous values only.}

\item{shape}{numeric or \code{svkey}. Categorical values may be provided, but
only up to 6 different values.}

\item{jitter}{numeric or \code{FALSE} (default). If a numeric value is provided,
a jitter will be applied to the points. Useful when many points are
directly overlaid. If provided, jitter is used as a numeric vector of length
2. If only one value is provided, that value will be recycled to length 2.}

\item{as_factor}{character. One or more of \code{"color"}, \code{"fill"}, \code{shape}.
Values supplied to the selected aesthetics will be made categorical via
\code{as.factor()} as the last step before plotting.}

\item{transforms}{(optional) named list of characters defining the transforms
to perform on specific aesthetics (e.g. x = "pseudo_log"). Transforms must
be understandable by \link[ggplot2:continuous_scale(transform)]{ggplot2::continuous_scale(transform)}}

\item{show_legend}{logical. show legend}

\item{cow_n_col}{cowplot param: how many columns}

\item{cow_rel_h}{cowplot param: relative heights of rows (e.g. c(1,2))}

\item{cow_rel_w}{cowplot param: relative widths of columns (e.g. c(1,2))}

\item{cow_align}{cowplot param: how to align}

\item{rasterize}{logical. Whether to rasterize with \code{scattermore}.}
}
\value{
a ggplot object
}
\description{
Create a scatter plot from \code{Giotto} object values. Aesthetic parameters
allow usage of \code{svkey()} to specify any values accessible with \code{spatValues}.
The main restriction is that retrieved values must map 1-to-1 with individual
cell_IDs. The points plotted will be based on the intersect of available
cell_IDs.
}
